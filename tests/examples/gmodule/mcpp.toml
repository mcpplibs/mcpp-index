# gnome.gmodule — dynamic loading, and the generated gmoduleconf.h behind it.
[indices]
gnome = { path = "../../.." }

[package]
name     = "gmodule-tests"
version  = "0.1.0"
standard = "c++23"

[target.'cfg(linux)'.dependencies.gnome]
gmodule = "2.82.5"
